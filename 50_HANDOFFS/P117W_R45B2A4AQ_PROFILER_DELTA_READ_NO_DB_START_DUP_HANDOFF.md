# P117W R45B2A4AQ — Handoff

State: CODE DELIVERY PRODUCED — OWNER VALIDATION REQUIRED

## Baseline

Owner-committed OPUS baseline:

`ce7348c87c8b2bf9e7ef6643a1df4d4fd313ad9e`

A4AP is committed. A4AQ addresses the separate multi-second performance regression reported immediately afterward.

## Evidence from owner runtime exports

The supplied runtime data shows a progressive divergence between actual business work and wrapper/request latency.

Examples include:

- front `/applications` requests in the multi-second range, reaching roughly 10 seconds in one captured request;
- back `registry-sync` / `registry-select` wrapper durations reaching roughly 0.8–3.0 seconds while their internal business-profiler traces can remain around tens to low hundreds of milliseconds;
- back profiler journal about 7.6 MB and front profiler journal about 3.9 MB at capture time.

The measured gap between the internal command trace duration and the surrounding in-process Composer `script.succeeded` duration grows strongly with the accumulated back journal size. This makes profiler persistence/transport part of the critical request path.

## Confirmed code causes

### Full retained-history scan on each profiled REST call

`RestClient` sends `X-Opus-Profiler: 1` automatically in development when a correlated trace id exists.

`RestServer` then stops its trace and calls `Profiler::readTrace($traceId)` to return remote profiler records to the front.

Current `Profiler::readTrace()` scans every retained JSONL file from the beginning and JSON-decodes every record. This is executed in the synchronous REST response path.

With multiple REST calls sharing one front trace id, later calls also reread/retransmit records produced by earlier calls.

### Duplicate database started event

`Trace::beginSpan()` automatically emits `<span>.started`.

`DatabaseOperationProfiler::measure()` independently emits the same `database.operation.started` again.

The owner's current registry-sync trace proves the duplication exactly: 31 database spans and 62 started events.

## A4AQ delivery

Artifact:

`opus_p117w_r45b2a4aq_profiler_delta_read_no_db_start_dup.zip`

SHA-256:

`077c29debf5224384b483e6f8df7d8ab0c0e788f99c8b070dff3e4de243b5298`

Two complete framework files:

1. `Opus/Profiler/Profiler.php`
2. `Opus/Database/DatabaseOperationProfiler.php`

No patcher. No deletion. No site-local file.

## Implementation

### `Opus\Profiler\Profiler`

- `start()` captures an opaque SHA-256 cursor representing the last retained JSONL record before the new trace scope;
- `stop()` records that cursor against the just-stopped trace id;
- the immediate `readTrace()` for the same trace id reads only records appended after that cursor;
- newest files are walked backwards in 64 KiB chunks, so historical journal body is not rescanned;
- the cursor survives normal JSONL rotation because it follows record content rather than filename/offset;
- an expired cursor is an explicit error, never a silent full-scan fallback;
- after the scoped read succeeds, later ordinary `readTrace()` calls retain the pre-existing full-history behavior required by the explicit Web Profiler route.

This means the REST back → front profiler exchange returns the records generated by that REST call only. Earlier records sharing the same correlation trace id are neither rescanned nor retransmitted.

### `Opus\Database\DatabaseOperationProfiler`

The redundant explicit `database.operation.started` call is removed because `Profiler::beginSpan()` / `Trace::beginSpan()` already records it.

All actual database measurements remain: span context, SQL/parameters where present, result/completion/failure events, span end, status and duration.

On the supplied registry-sync record, this removes 31 duplicate events and reduces that serialized trace by approximately 19% without removing a measured database operation.

## Pre-delivery smoke

Both delivered PHP files lint clean and contain no trailing whitespace.

Profiler delta smoke uses a copy of the owner's 7.6 MB back JSONL:

- an older record with the same trace id exists before `start()`;
- a nested child record is appended during the scope;
- `stop()` writes the parent record;
- first `readTrace()` returns exactly child + current parent, not the old same-trace record;
- second ordinary `readTrace()` returns the complete retained same-trace history;
- manual rotation between start and stop is passed.

The smoke therefore validates both non-duplication and preservation of explicit historical reads.

## Owner validation procedure

Do **not** clear profiler journals before the first A4AQ timing test. The regression is history-size dependent; clearing them first would hide whether the fix works.

After applying A4AQ, restart both OWASYS applications and hard-refresh the browser. Exercise at least:

1. Applications GET;
2. application selection/change;
3. Sources de données;
4. another Applications GET;
5. explicit Profiler trace opening.

Record the new front/back logs and compare elapsed wall time with the supplied pre-A4AQ capture.

Acceptance:

- ordinary navigation must no longer degrade simply because the JSONL history is large;
- one REST operation must transfer only its own new remote profiler records;
- a second REST operation under the same front trace must not retransmit the first operation's records;
- database started-event count must become one per database span;
- Profiler and Logger remain functional and mandatory;
- no FSM, REST contract, ACL, session or business behavior regression.

A4AQ deliberately does not change retention capacity or storage segmentation yet. If measurable append latency remains after removal of full-history rescans and duplicate BDD starts, that becomes a separately proven A4AR concern instead of mixing two storage policies into this fix.

Owner alone commits/pushes OPUS/OWASYS. Assistant writes MAESTRO_WORKSPACE only.
