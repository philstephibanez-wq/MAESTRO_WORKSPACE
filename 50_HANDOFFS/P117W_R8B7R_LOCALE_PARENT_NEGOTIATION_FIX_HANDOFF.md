# P117W R8B7R — Locale parent negotiation fix handoff

Date: 2026-09-04
Status: DELIVERABLE PREPARED — OWNER VALIDATION REQUIRED

## Authority

Apply `README-FIRST.md`, `00_COMMON_CONTRACTS/PATCH_DELIVERY_CONTRACT.md` and `00_COMMON_CONTRACTS/CHAT_NATIVE_ZIP_STEPWISE_WORKFLOW_CONTRACT.md`.

## Baseline

Expected OPUS baseline before extraction:

`a6c9acf3cbfd1a32f4aa5ebd96b2a4666b46850a` — `R8B7Q`.

## Delivered archive

`R8B7R.zip`

SHA-256:

`0a8be6a6a2ae1e838e206821e11d524fb91c7bb642afcd07cc1f8c14fc4c19b5`

Contents:

- `Opus/I18n/Locale.php`
- `Opus/I18n/LocaleInterface.php`

## Cause fixed

The generic browser locale negotiator invokes `Locale::parent()` while the locale value object did not provide that API. R8B7R supplies and contracts the missing locale-parent operation. No favicon-specific workaround is introduced.

## Owner validation gates

1. Baseline SHA and clean worktree.
2. Verify archive hash and contents.
3. Extract archive into `H:\OPUS`.
4. Regenerate optimized Composer autoload.
5. PHP lint both changed files.
6. Run both OWASYS site validations.
7. `git diff --check`, status and focused diff review.
8. Runtime: localized OWASYS front page plus browser `/favicon.ico` request; collect fresh front/back log and profiler evidence.
9. Commit/push OPUS only after runtime acceptance.

Any unexpected HEAD, dirty worktree, extraction mismatch, lint/validation failure or runtime regression is a stop condition.

## Next closure work after acceptance

R8B7R does not close the remaining I18n catalog warnings. Those warnings are handled separately after the 500 runtime defect is accepted, preserving one cause-level slice per delivery.

The post-closure roadmap remains: finish OPUS/OWASYS closure, documentation/RefBook, generated OPUS demo, then KB as an OPUS application generated by OWASYS with LSSTAR optional.
