# P117W R45B2A4BZ2R8A1 — Repeated dynamic ACL guard repair

State: DELIVERY INVALID — SUPERSEDED BY R8A1R1

## Failure discovered during owner validation

The runtime correction itself was valid, but the delivered applicator could not apply it to the actual R8A-generated working tree.

The R8A applicator produces `sites/owasys-front/application/default/services/FsmGuardHandlers.php` without a final newline. Its real SHA-256 is:

`2532c0fe5bfa6397a70dcb8a29adba636fee60a4d3d8f751b6802ec0d3b7b4d8`

R8A1 incorrectly preflighted the same textual content with a final newline and therefore expected:

`a677db775bfe4835d46549ae9148135bf75d77195c098db9f8ab0c892123568d`

Consequently R8A1 refused the owner's actual R8A working tree and changed nothing. The continued `OWASYS_EFSM_ACL_GUARD_NAMESPACE_RESERVED` failures were therefore still generated by the original R8A code.

## Runtime cause still requiring correction

`OwasysFsmGuardHandlers::forConfig()` must distinguish developer-managed guards from dynamic `acl:*` handlers synthesized while scanning transitions. Repeated references to one dynamic ACL guard are valid and idempotent.

## Superseding delivery

R8A1R1 uses the exact owner-observed baseline SHA `2532c0fe...`, applies the previously verified runtime correction, and verifies the corrected result SHA `e7c03e31...`.

R8B remains blocked until R8A + R8A1R1 owner validation passes.