# ASAP_VERSION — OBSOLETE / LEGACY POINTER

⚠️ Ce fichier n'est plus la source de vérité active.

Le framework actif est **OPUS Framework**.

La version active est :

```text
OPUS 8.1.0 "Lysenko"
```

La source de vérité active est désormais :

```text
CONTEXT/VERSIONS/OPUS_VERSION.md
```

## Statut de ASAP

`ASAP` est uniquement un ancien nom historique / legacy.

Il ne doit plus être utilisé comme nom actif du framework, ni dans les nouveaux handoffs, ni dans les nouvelles recettes, ni dans les nouvelles cibles de développement.

Les mentions restantes de `ASAP` doivent être lues comme :

```text
legacy / migration contrôlée / historique uniquement
```

Aucun fallback silencieux `ASAP -> OPUS` ou `OPUS -> ASAP` n'est autorisé.

## Règle de reprise

Pour toute nouvelle étape MAESTRO_WORKSPACE :

```text
Framework actif : OPUS Framework
Version cible : OPUS 8.1.0 "Lysenko"
ASAP : ancien nom historique, legacy uniquement
```
