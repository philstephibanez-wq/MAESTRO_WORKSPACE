# P115B_SERVER_LINUX_DOC_BASELINE

Patch workspace non destructif.

Objectif :
- documenter le futur serveur Linux Elite 800 G3 ;
- transformer la fiche PDF en base Markdown maintenable ;
- conserver la recommandation Debian stable + Xfce + SSH + xrdp + Apache/PHP ;
- préparer les futurs chapitres : DNS, HTTPS, Apache, PHP, maintenance.

Ce patch ne touche à aucun projet source.
