# P112Q3E3 TODO

- Validate on the real `H:\ASAP` tree.
- If the ROUTING directory contains additional live symbols, inspect the strict report and deliver a targeted live-symbol fix instead of weakening the contract.
- After validation, commit only `DOC`, `framework`, `tests`, `tools`, and `MANIFEST.md` changes.
- Keep generated `var/refbook/` and `var/reports/` ignored/untracked.
