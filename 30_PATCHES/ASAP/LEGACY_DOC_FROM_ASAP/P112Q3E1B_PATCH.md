# P112Q3E1B PATCH

- Replace `tests/Contract/run_refbook_fsm_metadata_contract_test.cmd` with an observable CRLF Windows wrapper.
- Ensure the wrapper runs from the ASAP root and preserves the PHP exit code.
